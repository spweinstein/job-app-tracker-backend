<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/head.ejs'); %>

  <body>
    <%- include('../partials/nav.ejs')%>
    
    <main class="page-container">
      <%- include('../partials/pageHeader.ejs'); %>
      <a class="btn btn-primary btn-lg" href="/calls/new">+ ADD CALL</a>

      <% if(calls.length>0){ %>
        <%- include('../partials/tables/table.ejs', {
          columnNames: ["Title", "State", "Company", "Start Date"],
          columnPaths: ["title", "state", "company.name", "startAt"],
          resourceName: "calls",
          rows: calls
        })%>

      <% } else { %>
        <p>No calls yet!</p>
      <% } %>
    </main>
  </body>
</html>
